[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["data", "ocr-service"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests"]
